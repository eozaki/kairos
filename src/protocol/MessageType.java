package protocol;
public enum MessageType {
    MSG_CONNECT,
    MSG_DISCONNECT,
    MSG_TIMEOUT,
    MSG_JOIN,
    MSG_CREATE,
    MSG_LEFT,
    MSG_PUBLISH,
    MSG_CONSUME,
    MSG_OK,
    MSG_ERROR
}
